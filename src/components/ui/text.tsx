import styled from 'styled-components';

const Text = styled.span`
  font-family: 'Inter', sans-serif;
  font-style: normal;
  font-weight: 800;
  font-size: 40px;
  line-height: 48px;

  color: #778898;
`;

export default Text