import styled from 'styled-components';
import {headerHeight} from "../ui/variables";

const Header = styled.header`
  width: 100%;
  height: ${headerHeight};
  padding-top: 25px;
`;

export default Header