import {StepPageProps} from "../step-pages.types";

export enum Subscription {
    NONE = 'NONE',
    FREE = 'FREE',
    MONTHLY = 'MONTHLY',
    YEARLY = 'YEARLY'
}

export interface SubscriptionTypeProps extends StepPageProps{

}